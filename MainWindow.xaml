<Window x:Class="WPF_PAR.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_PAR"
        xmlns:vm="clr-namespace:WPF_PAR.MVVM.ViewModels"
        xmlns:v="clr-namespace:WPF_PAR.MVVM.Views"
        xmlns:converters="clr-namespace:WPF_PAR.Core.Converters"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Sistema Principal - PAR Reportes" Height="768" Width="1280"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Background="#F4F6F9">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <v:SettingsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <v:DashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:FamiliaViewModel}">
            <v:FamiliasView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ClientesViewModel}">
            <v:ClientesView/>
        </DataTemplate>
        

        <Style x:Key="ModernNavButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
            <Setter Property="Height" Value="50"/>
            <Setter Property="Margin" Value="10,4"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="20,0,0,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="#546E7A"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="10"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                    <Setter Property="Foreground" Value="#1565C0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#B0BEC5"/>
            <Setter Property="Margin" Value="25,20,0,5"/>
            <Setter Property="Text" Value="{Binding RelativeSource={RelativeSource Self}, Path=Text}"/>
        </Style>

    </Window.Resources>

    <materialDesign:DrawerHost IsLeftDrawerOpen="{Binding IsMenuOpen}">
        <materialDesign:DrawerHost.LeftDrawerContent>
            <Grid Width="280" Background="White">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="20,30,20,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <materialDesign:Card UniformCornerRadius="25" Width="50" Height="50" materialDesign:ShadowAssist.Darken="True">
                        <Grid Background="{DynamicResource PrimaryHueMidBrush}">
                            <materialDesign:PackIcon Kind="Account" Width="30" Height="30" Foreground="White" 
                                                     HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </materialDesign:Card>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="15,0,0,0">
                        <TextBlock Text="Administrador" FontWeight="Bold" FontSize="15" Foreground="#263238"/>
                        <TextBlock Text="Gerente General" FontSize="12" Foreground="#90A4AE"/>
                    </StackPanel>

                    <Button Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-10,-10,0"
                            Style="{StaticResource MaterialDesignIconForegroundButton}"
                            Command="{x:Static materialDesign:DrawerHost.CloseDrawerCommand}"
                            ToolTip="Cerrar menú" Height="30" Width="30" Opacity="0.5">
                        <materialDesign:PackIcon Kind="ChevronLeft"/>
                    </Button>
                </Grid>

                <Separator Grid.Row="0" VerticalAlignment="Bottom" Margin="20,0" Background="#ECEFF1"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
                    <StackPanel>

                        <TextBlock Text="GENERAL" Style="{StaticResource SectionHeaderStyle}"/>

                        <Button Style="{StaticResource ModernNavButton}" Command="{Binding DashboardViewCommand}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ViewDashboardOutline" Width="22" Height="22" VerticalAlignment="Center" Margin="0,0,15,0"/>
                                <TextBlock Text="Dashboard" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <TextBlock Text="ANÁLISIS DE VENTAS" Style="{StaticResource SectionHeaderStyle}"/>

                        <Button Style="{StaticResource ModernNavButton}" Command="{Binding FamiliaViewCommand}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ChartBoxOutline" Width="22" Height="22" VerticalAlignment="Center" Margin="0,0,15,0"/>
                                <TextBlock Text="Familias y Productos" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource ModernNavButton}" Command="{Binding ClientesViewCommand}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="AccountGroupOutline" Width="22" Height="22" VerticalAlignment="Center" Margin="0,0,15,0"/>
                                <TextBlock Text="Cartera de Clientes" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                    </StackPanel>
                </ScrollViewer>

                <StackPanel Grid.Row="2" Margin="10,0,10,20">
                    <Separator Margin="10,10" Background="#ECEFF1"/>

                    <Button Style="{StaticResource ModernNavButton}" Command="{Binding SettingsViewCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CogOutline" Width="22" Height="22" VerticalAlignment="Center" Margin="0,0,15,0"/>
                            <TextBlock Text="Configuración" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Foreground="#EF5350">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource ModernNavButton}">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FFEBEE"/>
                                        <Setter Property="Foreground" Value="#D32F2F"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>

                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="LogoutVariant" Width="22" Height="22" VerticalAlignment="Center" Margin="0,0,15,0"/>
                            <TextBlock Text="Cerrar Sesión" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <TextBlock Text="v1.0.2 PAR Reportes" HorizontalAlignment="Center" FontSize="10" Foreground="#CFD8DC" Margin="0,10,0,0"/>
                </StackPanel>
            </Grid>
        </materialDesign:DrawerHost.LeftDrawerContent>

        <DockPanel>
            <materialDesign:ColorZone Padding="15" Mode="Standard" Background="White" 
                                      materialDesign:ShadowAssist.Darken="True"
                                      DockPanel.Dock="Top">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ToggleButton Style="{StaticResource MaterialDesignHamburgerToggleButton}" 
                                  IsChecked="{Binding IsMenuOpen}" 
                                  Width="40" Height="40" Foreground="#455A64"/>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20,0,0,0">
                        <TextBlock Text="PAR REPORTES" FontSize="18" FontWeight="Black" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Style="{StaticResource MaterialDesignIconForegroundButton}" ToolTip="Notificaciones">
                            <materialDesign:PackIcon Kind="BellOutline" Width="24" Height="24" Foreground="#78909C"/>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignIconForegroundButton}" ToolTip="Ayuda" Margin="5,0,0,0">
                            <materialDesign:PackIcon Kind="HelpCircleOutline" Width="24" Height="24" Foreground="#78909C"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </materialDesign:ColorZone>

            <ContentControl Content="{Binding CurrentView}" Margin="0"/>
        </DockPanel>
    </materialDesign:DrawerHost>
</Window>