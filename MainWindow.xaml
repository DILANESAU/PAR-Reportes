<Window x:Class="WPF_PAR.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_PAR"
        xmlns:vm="clr-namespace:WPF_PAR.MVVM.ViewModels"
        xmlns:v="clr-namespace:WPF_PAR.MVVM.Views"
        xmlns:models="clr-namespace:WPF_PAR.MVVM.Models"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="PAR Intelligence" Height="768" Width="1280"
        Icon="/Assets/logo.ico"
        WindowStartupLocation="CenterScreen"
        RenderOptions.BitmapScalingMode="HighQuality" 
        TextOptions.TextFormattingMode="Display"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Background="{DynamicResource MaterialDesignPaper}">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <v:SettingsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <v:DashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:FamiliaViewModel}">
            <v:FamiliasView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ClientesViewModel}">
            <v:ClientesView/>
        </DataTemplate>

        <Style x:Key="RailButtonStyle" TargetType="RadioButton" BasedOn="{StaticResource MaterialDesignFlatButton}">
            <Setter Property="Height" Value="50"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
            <Setter Property="GroupName" Value="MenuGroup"/>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PrimaryHueLightBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Agregado: Definición del recurso faltante para evitar el error XDG0010 -->
        <Style x:Key="MaterialDesignSnackbarActionMidButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}" />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{DynamicResource MaterialDesignPaper}" 
                BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,1,0">

            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Width" Value="65"/>
                    <Style.Triggers>
                        <EventTrigger RoutedEvent="MouseEnter">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Width" To="240" Duration="0:0:0.25">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                        <EventTrigger RoutedEvent="MouseLeave">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Width" To="65" Duration="0:0:0.25">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" Margin="18,30,0,30">
                    <materialDesign:PackIcon Kind="ChartBubble" Width="30" Height="30" 
                                             Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    <TextBlock Text="PAR Intelligence" VerticalAlignment="Center" Margin="20,0,0,0" 
                               FontWeight="Bold" FontSize="16" TextTrimming="CharacterEllipsis"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="0,10">

                    <RadioButton Style="{StaticResource RailButtonStyle}" Command="{Binding DashboardViewCommand}" IsChecked="True">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ViewDashboardOutline" Width="24" Height="24" Margin="20,0,25,0"/>
                            <TextBlock Text="Dashboard" VerticalAlignment="Center"/>
                        </StackPanel>
                    </RadioButton>

                    <RadioButton Style="{StaticResource RailButtonStyle}" Command="{Binding FamiliaViewCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ChartPie" Width="24" Height="24" Margin="20,0,25,0"/>
                            <TextBlock Text="Análisis Familias" VerticalAlignment="Center"/>
                        </StackPanel>
                    </RadioButton>

                    <RadioButton Style="{StaticResource RailButtonStyle}" Command="{Binding ClientesViewCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="AccountGroupOutline" Width="24" Height="24" Margin="20,0,25,0"/>
                            <TextBlock Text="Cartera de Clientes" VerticalAlignment="Center"/>
                        </StackPanel>
                    </RadioButton>

                </StackPanel>

                <StackPanel Grid.Row="2" Margin="0,0,0,20">
                    <RadioButton Style="{StaticResource RailButtonStyle}" Command="{Binding SettingsViewCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CogOutline" Width="24" Height="24" Margin="20,0,25,0"/>
                            <TextBlock Text="Configuración" VerticalAlignment="Center"/>
                        </StackPanel>
                    </RadioButton>

                    <Separator Margin="10,10,10,20" Background="{DynamicResource MaterialDesignDivider}"/>

                    <StackPanel Orientation="Horizontal" Margin="10,0,10,10" VerticalAlignment="Bottom">

                        <materialDesign:PackIcon Kind="AccountCircle" Width="40" Height="40" 
                             Foreground="{DynamicResource PrimaryHueLightBrush}" 
                             VerticalAlignment="Center" Margin="0,0,10,0"/>

                        <StackPanel VerticalAlignment="Center">

                            <TextBlock Text="{Binding UserName}" 
                                FontWeight="Bold" 
                                FontSize="14"
                                Foreground="{DynamicResource MaterialDesignBody}"
                                TextTrimming="CharacterEllipsis" 
                                MaxWidth="150"/>
                            <TextBlock Text="{Binding UserRol}" 
                                FontSize="11" 
                                Opacity="0.6"
                                Foreground="{DynamicResource MaterialDesignBody}"/>

                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <DockPanel Grid.Column="1">

            <materialDesign:DialogHost Identifier="RootDialog">
                <Grid>

                    <materialDesign:TransitioningContent 
                        Content="{Binding CurrentView}"
                        Margin="20,0,20,20">

                        <materialDesign:TransitioningContent.OpeningEffect>
                            <materialDesign:TransitionEffect Kind="SlideInFromRight" Duration="0:0:0.4" />
                        </materialDesign:TransitioningContent.OpeningEffect>

                    </materialDesign:TransitioningContent>

                    <materialDesign:Snackbar
                        MessageQueue="{Binding MessageQueue}"
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Bottom"
                        Margin="0,0,0,20"
                        Background="#333333"
                        Foreground="White"
                        ActionButtonStyle="{StaticResource MaterialDesignSnackbarActionMidButton}" />
                </Grid>
            </materialDesign:DialogHost>
        </DockPanel>
    </Grid>
</Window>