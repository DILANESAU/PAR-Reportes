<Window x:Class="WPF_PAR.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_PAR"
        xmlns:vm="clr-namespace:WPF_PAR.MVVM.ViewModels"
        xmlns:v="clr-namespace:WPF_PAR.MVVM.Views"
        xmlns:models="clr-namespace:WPF_PAR.MVVM.Models"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Sistema Principal - PAR Reportes" Height="768" Width="1280"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Background="{DynamicResource MaterialDesignBackground}">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <v:SettingsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <v:DashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:FamiliaViewModel}">
            <v:FamiliasView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ClientesViewModel}">
            <v:ClientesView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type models:NotificationAlert}">
            <materialDesign:Card MinWidth="350" MaxWidth="500" MinHeight="65"
                         UniformCornerRadius="8" Padding="0" Margin="0" 
                         materialDesign:ElevationAssist.Elevation="Dp6"
                         TextElement.Foreground="{DynamicResource MaterialDesignBody}">

                <materialDesign:Card.RenderTransform>
                    <TranslateTransform Y="-20"/>
                </materialDesign:Card.RenderTransform>

                <materialDesign:Card.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.Opacity)"
                                         From="0" To="1" Duration="0:0:0.3"/>

                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                         From="-20" To="0" Duration="0:0:0.3">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </materialDesign:Card.Triggers>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border x:Name="IconBorder" Background="#4CAF50" CornerRadius="8,0,0,8" VerticalAlignment="Stretch">
                        <materialDesign:PackIcon x:Name="AlertIcon" Kind="CheckCircle" Width="28" Height="28" Foreground="White" 
                                         HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <StackPanel Grid.Column="1" Margin="15,10" VerticalAlignment="Center">
                        <TextBlock x:Name="TitleText" Text="{Binding Title}" FontWeight="Bold" FontSize="15" Margin="0,0,0,3"/>
                        <TextBlock Text="{Binding Message}" FontSize="13" Opacity="0.8" TextWrapping="Wrap" LineHeight="18"/>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>

            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding Type}" Value="Error">
                    <Setter TargetName="IconBorder" Property="Background" Value="#E53935"/>
                    <Setter TargetName="AlertIcon" Property="Kind" Value="CloseCircleOutline"/>
                    <Setter TargetName="TitleText" Property="Foreground" Value="#C62828"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Type}" Value="Warning">
                    <Setter TargetName="IconBorder" Property="Background" Value="#FF9800"/>
                    <Setter TargetName="AlertIcon" Property="Kind" Value="AlertOutline"/>
                    <Setter TargetName="TitleText" Property="Foreground" Value="#EF6C00"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>

        <Style x:Key="ModernNavButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
            <Setter Property="Height" Value="50"/>
            <Setter Property="Margin" Value="10,4"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="20,0,0,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="10"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Opacity" Value="0.7"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PrimaryHueLightBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}"/>
                    <Setter Property="Opacity" Value="1"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
            <Setter Property="Opacity" Value="0.4"/>
            <Setter Property="Margin" Value="25,20,0,5"/>
        </Style>
    </Window.Resources>

    <materialDesign:DrawerHost IsLeftDrawerOpen="{Binding IsMenuOpen, Mode=TwoWay}">
        <materialDesign:DrawerHost.LeftDrawerContent>
            <Grid Width="280" Background="{DynamicResource MaterialDesignPaper}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="20,30,20,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <materialDesign:Card UniformCornerRadius="25" Width="50" Height="50" materialDesign:ElevationAssist.Elevation="Dp2">
                        <Grid Background="{DynamicResource PrimaryHueMidBrush}">
                            <materialDesign:PackIcon Kind="Account" Width="30" Height="30" Foreground="White" 
                                                     HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                    </materialDesign:Card>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="15,0,0,0">
                        <TextBlock Text="Administrador" FontWeight="Bold" FontSize="15"/>
                        <TextBlock Text="Gerente General" FontSize="12" Opacity="0.6"/>
                    </StackPanel>

                    <Button Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-10,-10,0"
                            Style="{StaticResource MaterialDesignIconForegroundButton}"
                            Command="{x:Static materialDesign:DrawerHost.CloseDrawerCommand}"
                            ToolTip="Cerrar menú" Height="30" Width="30" Opacity="0.5">
                        <materialDesign:PackIcon Kind="ChevronLeft"/>
                    </Button>
                </Grid>

                <Separator Grid.Row="0" VerticalAlignment="Bottom" Margin="20,0" Opacity="0.1"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
                    <StackPanel>
                        <TextBlock Text="GENERAL" Style="{StaticResource SectionHeaderStyle}"/>

                        <Button Style="{StaticResource ModernNavButton}" Command="{Binding DashboardViewCommand}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ViewDashboardOutline" Width="22" Height="22" VerticalAlignment="Center" Margin="0,0,15,0"/>
                                <TextBlock Text="Dashboard" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <TextBlock Text="ANÁLISIS DE VENTAS" Style="{StaticResource SectionHeaderStyle}"/>

                        <Button Style="{StaticResource ModernNavButton}" Command="{Binding FamiliaViewCommand}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ChartBoxOutline" Width="22" Height="22" VerticalAlignment="Center" Margin="0,0,15,0"/>
                                <TextBlock Text="Familias y Productos" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource ModernNavButton}" Command="{Binding ClientesViewCommand}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="AccountGroupOutline" Width="22" Height="22" VerticalAlignment="Center" Margin="0,0,15,0"/>
                                <TextBlock Text="Cartera de Clientes" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </ScrollViewer>

                <StackPanel Grid.Row="2" Margin="10,0,10,20">
                    <Separator Margin="10,10" Opacity="0.1"/>

                    <Button Style="{StaticResource ModernNavButton}" Command="{Binding SettingsViewCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CogOutline" Width="22" Height="22" VerticalAlignment="Center" Margin="0,0,15,0"/>
                            <TextBlock Text="Configuración" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Foreground="#E57373">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource ModernNavButton}">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#30F44336"/>
                                        <Setter Property="Foreground" Value="#D32F2F"/>
                                        <Setter Property="Opacity" Value="1"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>

                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="LogoutVariant" Width="22" Height="22" VerticalAlignment="Center" Margin="0,0,15,0"/>
                            <TextBlock Text="Cerrar Sesión" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <TextBlock Text="v1.0.2 PAR Reportes" HorizontalAlignment="Center" FontSize="10"
                               Foreground="{DynamicResource MaterialDesignBody}" Opacity="0.3" Margin="0,10,0,0"/>
                </StackPanel>
            </Grid>
        </materialDesign:DrawerHost.LeftDrawerContent>

        <DockPanel>
            <materialDesign:ColorZone Padding="15" Mode="Standard" 
                                      Background="{DynamicResource MaterialDesignPaper}" 
                                      materialDesign:ElevationAssist.Elevation="Dp4"
                                      DockPanel.Dock="Top">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ToggleButton Style="{StaticResource MaterialDesignHamburgerToggleButton}" 
                                  IsChecked="{Binding IsMenuOpen, Mode=TwoWay}" 
                                  Width="40" Height="40" 
                                  Foreground="{DynamicResource MaterialDesignBody}"/>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20,0,0,0">
                        <TextBlock Text="PAR REPORTES" FontSize="18" FontWeight="Black" 
                                   Foreground="{DynamicResource MaterialDesignBody}" />
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Style="{StaticResource MaterialDesignIconForegroundButton}" ToolTip="Notificaciones">
                            <materialDesign:PackIcon Kind="BellOutline" Width="24" Height="24" Opacity="0.6"/>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignIconForegroundButton}" ToolTip="Ayuda" Margin="10,0,0,0">
                            <materialDesign:PackIcon Kind="HelpCircleOutline" Width="24" Height="24" Opacity="0.6"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </materialDesign:ColorZone>

            <materialDesign:DialogHost Identifier="RootDialog">
                <Grid>
                    <ContentControl Content="{Binding CurrentView}" Margin="0"/>

                    <materialDesign:Snackbar x:Name="MainSnackbar"
                         HorizontalAlignment="Right"
                         VerticalAlignment="Bottom"
                         Margin="0,0,24,24"
                         Background="Transparent">
                        <materialDesign:Snackbar.Resources>
                            <Style TargetType="{x:Type materialDesign:SnackbarMessage}">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>

                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type materialDesign:SnackbarMessage}">
                                            <Grid>
                                                <ContentPresenter Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              HorizontalAlignment="Stretch"
                                              VerticalAlignment="Stretch"/>
                                            </Grid>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>

                                <Setter Property="Margin" Value="0"/>
                                <Setter Property="Padding" Value="0"/>
                            </Style>
                        </materialDesign:Snackbar.Resources>
                    </materialDesign:Snackbar>
                </Grid>
            </materialDesign:DialogHost>
        </DockPanel>

    </materialDesign:DrawerHost>
</Window>