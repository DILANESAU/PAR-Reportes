<UserControl x:Class="WPF_PAR.MVVM.Views.FamiliasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <materialDesign:TransitioningContent OpeningEffect="{materialDesign:TransitionEffect FadeIn}">
            <Grid Visibility="{Binding VerResumen, Converter={StaticResource BoolToVis}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <materialDesign:Card Grid.Row="0" Margin="0,0,0,20" Padding="15" UniformCornerRadius="8">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="FilterVariant" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="Filtros:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,20,0"/>

                        <ComboBox materialDesign:HintAssist.Hint="Sucursal" Width="180" Margin="0,0,15,0"
                      Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                      ItemsSource="{Binding ListaSucursales}"
                      DisplayMemberPath="Value"
                      SelectedValuePath="Key"
                      SelectedValue="{Binding SucursalSeleccionadaId}"/>

                        <ComboBox materialDesign:HintAssist.Hint="Año" Width="80" Margin="0,0,15,0"
                      Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                      ItemsSource="{Binding ListaAnios}"
                      SelectedItem="{Binding AnioSeleccionado}"/>

                        <ComboBox materialDesign:HintAssist.Hint="Mes" Width="120" Margin="0,0,20,0"
                      Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                      ItemsSource="{Binding ListaMeses}"
                      DisplayMemberPath="Value"
                      SelectedValuePath="Key"
                      SelectedValue="{Binding MesSeleccionado}"/>

                        <Button Content="Consultar" 
                    Command="{Binding ActualizarCommand}"
                    Height="32" Padding="20,0" Margin="10,0,0,0"/>
                    </StackPanel>
                </materialDesign:Card>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding TarjetasFamilias}">

                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <materialDesign:Card Width="300" Margin="0,0,20,20" UniformCornerRadius="12">
                                    <Grid>
                                        <Border Background="{Binding ColorFondo}" CornerRadius="12"/>

                                        <Border CornerRadius="12" Opacity="0.2">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="White" Offset="0"/>
                                                    <GradientStop Color="Transparent" Offset="0.4"/>
                                                    <GradientStop Color="Transparent" Offset="0.6"/>
                                                    <GradientStop Color="Black" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.Background>
                                        </Border>

                                        <StackPanel Margin="20">
                                            <Grid Margin="0,0,0,15">
                                                <TextBlock Text="{Binding NombreFamilia}" FontSize="22" FontWeight="Bold" Foreground="White"/>
                                                <materialDesign:PackIcon Kind="ChartBox" Foreground="White" HorizontalAlignment="Right" Width="24" Height="24" Opacity="0.5"/>
                                            </Grid>

                                            <Separator Background="White" Opacity="0.3" Margin="0,0,0,15"/>

                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                                <materialDesign:PackIcon Kind="CurrencyUsd" Foreground="White" VerticalAlignment="Center" Width="25" Height="25"/>
                                                <StackPanel Margin="10,0,0,0">
                                                    <TextBlock Text="Venta Total" Foreground="{Binding ColorTexto}" Opacity="0.8" FontSize="11"/>
                                                    <TextBlock Text="{Binding VentaTotal, StringFormat=C2}" Foreground="White" FontSize="20" FontWeight="Bold"/>
                                                </StackPanel>
                                            </StackPanel>

                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                                <materialDesign:PackIcon Kind="Oil" Foreground="White" VerticalAlignment="Center" Width="25" Height="25"/>
                                                <StackPanel Margin="10,0,0,0">
                                                    <TextBlock Text="Litros Totales" Foreground="{Binding ColorTexto}" Opacity="0.8" FontSize="11"/>
                                                    <TextBlock Text="{Binding LitrosTotales, StringFormat={}{0:N1} Lts}" Foreground="White" FontSize="18"/>
                                                </StackPanel>
                                            </StackPanel>

                                            <Border Background="#30000000" CornerRadius="6" Padding="10">
                                                <StackPanel>
                                                    <StackPanel Orientation="Horizontal">
                                                        <materialDesign:PackIcon Kind="Crown" Foreground="#FFD700" Margin="0,0,5,0"/>
                                                        <TextBlock Text="Cliente Top" Foreground="{Binding ColorTexto}" Opacity="0.9" FontSize="12" FontWeight="SemiBold"/>
                                                    </StackPanel>
                                                    <TextBlock Text="{Binding MejorCliente}" Foreground="{Binding ColorTexto}" TextTrimming="CharacterEllipsis" ToolTip="{Binding MejorCliente}" FontSize="14" Margin="0,5,0,0"/>
                                                </StackPanel>
                                            </Border>

                                            <Border Background="#30000000" CornerRadius="6" Padding="10"/>
                                            <Border Background="#30000000" CornerRadius="6" Padding="10" Margin="0,10,0,0">
                                                <StackPanel>
                                                    <StackPanel Orientation="Horizontal">
                                                        <materialDesign:PackIcon Kind="StarCircle" Foreground="#FFD700" Margin="0,0,5,0"/>
                                                        <TextBlock Text="Producto + vendido" Foreground="{Binding ColorTexto}" Opacity="0.9" FontSize="12" FontWeight="SemiBold"/>
                                                    </StackPanel>
                                                    <TextBlock Text="{Binding ProductoEstrella}"
                                               Foreground="{Binding ColorTexto}"
                                               TextTrimming="CharacterEllipsis"
                                               ToolTip="{Binding ProductoEstrella}"
                                               FontSize="13" Margin="0,5,0,0"
                                               TextWrapping="NoWrap"/>
                                                </StackPanel>

                                            </Border>

                                            <Button Margin="0,20,0,0"
                                        Style="{StaticResource MaterialDesignOutlinedButton}"
                                        Foreground="{Binding ColorTexto}" BorderBrush="White"
                                        Content="VER DETALLE"
                                        Command="{Binding DataContext.VerDetalleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding NombreFamilia}"/>
                                        </StackPanel>
                                    </Grid>
                                </materialDesign:Card>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </materialDesign:TransitioningContent>
        
        <materialDesign:TransitioningContent OpeningEffect="{materialDesign:TransitionEffect Kind=SlideInFromLeft}">
            <Grid Visibility="{Binding VerDetalle, Converter={StaticResource BoolToVis}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <materialDesign:Card Grid.Row="0" Margin="0,0,0,20" Padding="15" UniformCornerRadius="8">
                    <Grid>
                        <StackPanel Orientation="Horizontal">
                            <Button Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                        ToolTip="Regresar"
                        Command="{Binding RegresarCommand}"
                        Background="{DynamicResource PrimaryHueMidBrush}"
                        BorderBrush="{DynamicResource PrimaryHueMidBrush}">
                                <materialDesign:PackIcon Kind="ArrowLeft"/>
                            </Button>

                            <StackPanel Margin="20,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding TituloDetalle}" Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
                                <TextBlock Text="Listado detallado de movimientos" Opacity="0.6" FontSize="12"/>
                            </StackPanel>
                        </StackPanel>
                        <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                    Command="{Binding ExportarExcelCommand}"
                    Padding="12 6"
                    HorizontalAlignment="Right">

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <materialDesign:PackIcon Kind="FileExcel" Width="20" Height="20"/>
                                <TextBlock Grid.Column="2" Text="Exportar Excel" VerticalAlignment="Center"/>
                            </Grid>
                        </Button>
                        <TextBox Width="500"
                    Margin="50,5,200,10"
                     FontSize="15"
                    materialDesign:HintAssist.Hint="Buscar folio, cliente, producto..."
                    Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                    Text="{Binding TextoBusqueda, UpdateSourceTrigger=PropertyChanged}">
                        </TextBox>
                    </Grid>
                </materialDesign:Card>

                <materialDesign:Card Grid.Row="1" VerticalAlignment="Stretch" UniformCornerRadius="8" Padding="0">

                    <DataGrid ItemsSource="{Binding DetalleVentas}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  SelectionMode="Single"
                  VerticalScrollBarVisibility="Auto">


                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Fecha" Binding="{Binding FechaEmision, StringFormat=dd/MM/yyyy}" Width="100"/>
                            <DataGridTextColumn Header="Sucursal" Binding="{Binding Sucursal}" Width="80"/>
                            <DataGridTextColumn Header="Folio" Binding="{Binding MovID}" Width="100"/>
                            <DataGridTextColumn Header="Clave" Binding="{Binding Articulo}" Width="120" FontWeight="SemiBold"/>
                            <DataGridTextColumn Header="Producto" Binding="{Binding Descripcion}" Width="250">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                        <Setter Property="ToolTip" Value="{Binding Descripcion}"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Cliente" Binding="{Binding Cliente}" Width="250"/>
                            <DataGridTextColumn Header="Cant." Binding="{Binding Cantidad, StringFormat=N0}" Width="60"/>
                            <DataGridTextColumn Header="Lts Tot." Binding="{Binding LitrosTotales, StringFormat=N1}" Width="80"/>
                            <DataGridTextColumn Header="Total Venta" Binding="{Binding TotalVenta, StringFormat=C2}" Width="120" FontWeight="Bold">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="Padding" Value="0,0,10,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </materialDesign:Card>
            </Grid>
        </materialDesign:TransitioningContent>

        
        <Grid Background="#80FFFFFF" 
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
              d:Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                             Value="0"
                             IsIndeterminate="True"
                             Width="60" Height="60"/>

                <TextBlock Text="Analizando ventas..." 
                           HorizontalAlignment="Center" 
                           Margin="0,15,0,0" 
                           FontWeight="Bold" 
                           Foreground="{DynamicResource PrimaryHueMidBrush}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>