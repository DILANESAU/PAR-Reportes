<UserControl x:Class="WPF_PAR.MVVM.Views.FamiliasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:converters="clr-namespace:WPF_PAR.Core.Converters" 
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1600"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="Transparent"
             FontFamily="{DynamicResource MaterialDesignFont}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:BoolToColorConverter x:Key="BoolToColorConverter"/>
        <converters:BoolToOpacityConverter x:Key="BoolToOpacityConverter"/>

        <Style x:Key="HoverCardStyle" TargetType="materialDesign:Card">
            <Setter Property="Background" Value="{DynamicResource MaterialDesignPaper}"/>
            <Setter Property="UniformCornerRadius" Value="16"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Width" Value="320"/>
            <Setter Property="Height" Value="180"/>
            <Setter Property="materialDesign:ShadowAssist.Darken" Value="True"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp8"/>
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1.03" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1.03" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ContentCardStyle" TargetType="materialDesign:Card">
            <Setter Property="UniformCornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp2"/>
        </Style>

        <DataTemplate x:Key="FamiliaCardTemplate">
            <materialDesign:Card Style="{StaticResource HoverCardStyle}">
                <Button Command="{Binding DataContext.VerDetalleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding NombreFamilia}"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Padding="0" Height="Auto" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1.5*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" Background="{Binding ColorFondo}" CornerRadius="16,16,0,0">
                            <Grid Margin="20">
                                <TextBlock Text="{Binding NombreFamilia}" FontSize="24" FontWeight="Bold" Foreground="{Binding ColorTexto}" 
                                           TextTrimming="CharacterEllipsis" VerticalAlignment="Bottom"/>
                                <materialDesign:PackIcon Kind="ChartBox" Foreground="{Binding ColorTexto}" Opacity="0.3" Width="60" Height="60" 
                                                         HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-10,-10,0"/>
                            </Grid>
                        </Border>
                        <Border Grid.Row="1" Background="White" CornerRadius="0,0,16,16">
                            <Grid Margin="20,10">
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="VENTA" FontSize="11" Opacity="0.5" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding VentaTotal, StringFormat=C2}" FontSize="22" FontWeight="SemiBold"/>
                                </StackPanel>
                                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <TextBlock Text="VOLUMEN" FontSize="11" Opacity="0.5" FontWeight="Bold" HorizontalAlignment="Right"/>
                                    <TextBlock Text="{Binding LitrosTotales, StringFormat={}{0:N0} L}" FontSize="16" Opacity="0.8" HorizontalAlignment="Right"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </Grid>
                </Button>
            </materialDesign:Card>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="30">

        <materialDesign:TransitioningContent OpeningEffect="{materialDesign:TransitionEffect FadeIn}">
            <Grid Visibility="{Binding VerResumen, Converter={StaticResource BoolToVis}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,25">
                    <TextBlock Text="Panorama de Familias" Style="{StaticResource PageTitleStyle}"/>
                    <TextBlock Text="Rendimiento general por categorías" Style="{StaticResource PageSubtitleStyle}"/>
                </StackPanel>

                <materialDesign:Card Grid.Row="1" Style="{StaticResource FilterBarStyle}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="FilterVariant" VerticalAlignment="Center" Margin="0,0,10,0" Opacity="0.5"/>

                        <ComboBox ItemsSource="{Binding Filters.ListaSucursales}" 
                  SelectedValue="{Binding Filters.SucursalId}"
                  DisplayMemberPath="Value" 
                  SelectedValuePath="Key"
                  Width="200" Margin="0,0,20,0" 
                  materialDesign:HintAssist.Hint="Sucursal" 
                  Style="{StaticResource MaterialDesignOutlinedComboBox}"/>

                        <DatePicker SelectedDate="{Binding Filters.FechaInicio}" Width="130" Margin="0,0,10,0" 
                    materialDesign:HintAssist.Hint="Desde" Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>

                        <DatePicker SelectedDate="{Binding Filters.FechaFin}" Width="130" Margin="0,0,20,0" 
                    materialDesign:HintAssist.Hint="Hasta" Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>

                        <Button Command="{Binding ActualizarCommand}" Style="{StaticResource MaterialDesignRaisedButton}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Refresh" Margin="0,0,8,0"/>
                                <TextBlock Text="Actualizar"/>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding ExportarGlobalCommand}" Style="{StaticResource MaterialDesignOutlinedButton}"
                Margin="50,0,0,0" BorderBrush="#43A047" Foreground="#2E7D32" ToolTip="Descargar reporte global">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FileExcel" Margin="0,0,8,0"/>
                                <TextBlock Text="Exportar Todo"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </materialDesign:Card>

                <UniformGrid Grid.Row="2" Rows="1" Margin="0,0,0,30" Height="140">
                    <materialDesign:Card Margin="0,0,20,0" UniformCornerRadius="16" Background="#2E7D32" Foreground="White" materialDesign:ShadowAssist.Darken="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <materialDesign:PackIcon Kind="CurrencyUsd" Grid.Column="1" Width="100" Height="100" HorizontalAlignment="Right" VerticalAlignment="Bottom" Opacity="0.15" Margin="0,0,-20,-20"/>
                            <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="30,0,0,0">
                                <TextBlock Text="VENTA TOTAL PERIODO" Opacity="0.8" FontWeight="Bold"/>
                                <Viewbox StretchDirection="DownOnly" HorizontalAlignment="Left" Height="60">
                                    <TextBlock Text="{Binding GranTotalVenta, StringFormat=C2}" FontWeight="Bold"/>
                                </Viewbox>
                            </StackPanel>
                        </Grid>
                    </materialDesign:Card>
                    <materialDesign:Card Margin="0" UniformCornerRadius="16" Background="#263238" Foreground="White" materialDesign:ShadowAssist.Darken="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <materialDesign:PackIcon Kind="Beaker" Grid.Column="1" Width="100" Height="100" HorizontalAlignment="Right" VerticalAlignment="Bottom" Opacity="0.15" Margin="0,0,-20,-20"/>
                            <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="30,0,0,0">
                                <TextBlock Text="VOLUMEN TOTAL (LTS)" Opacity="0.8" FontWeight="Bold" Foreground="#FFCC80"/>
                                <Viewbox StretchDirection="DownOnly" HorizontalAlignment="Left" Height="60">
                                    <TextBlock Text="{Binding GranTotalLitros, StringFormat={}{0:N0}}" FontWeight="Bold" Foreground="#FFCC80"/>
                                </Viewbox>
                            </StackPanel>
                        </Grid>
                    </materialDesign:Card>
                </UniformGrid>

                <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" Padding="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Categorías Disponibles" HorizontalAlignment="Center" Opacity="0.4" Margin="0,0,0,15" FontWeight="Bold"/>
                        <ItemsControl ItemsSource="{Binding TarjetasFamilias}" ItemTemplate="{StaticResource FamiliaCardTemplate}" HorizontalAlignment="Center" MaxWidth="1600">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </materialDesign:TransitioningContent>

        <materialDesign:TransitioningContent OpeningEffect="{materialDesign:TransitionEffect SlideInFromRight}">
            <Grid Visibility="{Binding VerDetalle, Converter={StaticResource BoolToVis}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,10">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="Detalle:" Style="{StaticResource PageTitleStyle}" Foreground="#90A4AE" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding TituloDetalle}" Style="{StaticResource PageTitleStyle}"/>
                    </StackPanel>

                    <materialDesign:Card Style="{StaticResource FilterBarStyle}" Margin="0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Button Command="{Binding RegresarCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" ToolTip="Volver al resumen">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="ArrowLeft" Margin="0,0,8,0"/>
                                    <TextBlock Text="Volver"/>
                                </StackPanel>
                            </Button>

                            <StackPanel Grid.Column="2" Orientation="Horizontal">
                                <ComboBox ItemsSource="{Binding SubLineasDisponibles}" SelectedItem="{Binding SubLineaSeleccionada}"
                                          Style="{StaticResource MaterialDesignOutlinedComboBox}" 
                                          Width="250" Margin="0,0,15,0" materialDesign:HintAssist.Hint="Filtrar por Sub-Línea"/>

                                <Button Command="{Binding ExportarExcelCommand}" Style="{StaticResource MaterialDesignRaisedButton}">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="FileExcel" Margin="0,0,8,0"/>
                                        <TextBlock Text="Exportar CSV"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </materialDesign:Card>
                </StackPanel>

                <materialDesign:Card Grid.Row="1" UniformCornerRadius="8" Padding="0">
                    <TabControl materialDesign:ColorZoneAssist.Mode="PrimaryMid" Style="{StaticResource MaterialDesignFilledTabControl}">

                        <TabItem>
                            <TabItem.Header>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="ChartPie" Margin="0,0,8,0"/>
                                    <TextBlock Text="ANÁLISIS VISUAL"/>
                                </StackPanel>
                            </TabItem.Header>

                            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="15">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <Grid Grid.Row="0" Margin="0,0,0,20" MinHeight="350">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <materialDesign:Card Grid.Column="0" Margin="0,0,10,0" Style="{StaticResource ContentCardStyle}">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="*"/>
                                                </Grid.RowDefinitions>
                                                <TextBlock Text="{Binding TituloGraficoPastel}" FontWeight="Bold" Opacity="0.6" HorizontalAlignment="Center"/>
                                                <lvc:PieChart Grid.Row="1" Series="{Binding SeriesPastelDinero}" LegendPosition="Right" TooltipPosition="Center" AnimationsSpeed="0:0:0.3" LegendTextSize="12"/>
                                            </Grid>
                                        </materialDesign:Card>

                                        <materialDesign:Card Grid.Column="1" Margin="10,0,0,0" Style="{StaticResource ContentCardStyle}">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="*"/>
                                                </Grid.RowDefinitions>
                                                <TextBlock Text="Top Volumen (Litros)" FontWeight="Bold" Opacity="0.6" HorizontalAlignment="Center"/>
                                                <lvc:PieChart Grid.Row="1" Series="{Binding SeriesPastelLitros}" LegendPosition="Right" TooltipPosition="Center" AnimationsSpeed="0:0:0.3" LegendTextSize="12"/>
                                            </Grid>
                                        </materialDesign:Card>
                                    </Grid>

                                    <materialDesign:Card Grid.Row="1" Margin="0,0,0,10" Style="{StaticResource ContentCardStyle}" MinHeight="450">
                                        <Grid Grid.Row="1" Margin="0,0,0,10">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>

                                            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,10,10">
                                                <StackPanel Orientation="Horizontal" Margin="0,0,15,0" VerticalAlignment="Center">
                                                    <TextBlock Text="Excluir Blancos" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="12" Opacity="0.7"/>
                                                    <ToggleButton IsChecked="{Binding ExcluirBlancos}" 
                                                        Style="{StaticResource MaterialDesignSwitchToggleButton}"
                                                        ToolTip="Ocultar productos blancos para analizar colores"/>
                                                </StackPanel>
                                                <TextBlock Text="Mostrar Top:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold" Opacity="0.6"/>
                                                <ComboBox ItemsSource="{Binding OpcionesTop}" SelectedItem="{Binding TopSeleccionado}"
                                                    Style="{StaticResource MaterialDesignOutlinedComboBox}" Width="80"
                                                      materialDesign:TextFieldAssist.TextFieldCornerRadius="4"/>
                                            </StackPanel>

                                            <Grid Grid.Row="1" MinHeight="{Binding AlturaGraficaDinamica}">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="20"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <materialDesign:Card Grid.Column="0" Style="{StaticResource ContentCardStyle}">
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="*"/>
                                                        </Grid.RowDefinitions>

                                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                                            <materialDesign:PackIcon Kind="AccountTie" VerticalAlignment="Center" Margin="0,0,10,0" Opacity="0.5"/>
                                                            <TextBlock Text="{Binding TituloBarrasClientes}" FontWeight="Bold" FontSize="16"/>
                                                        </StackPanel>

                                                        <lvc:CartesianChart Grid.Row="1" 
                                                            Series="{Binding SeriesBarrasClientes}" 
                                                            XAxes="{Binding EjeXBarrasClientes}" 
                                                            YAxes="{Binding EjeYBarrasClientes}"
                                                            ZoomMode="X" AnimationsSpeed="0:0:0.5"/>
                                                    </Grid>
                                                </materialDesign:Card>

                                                <materialDesign:Card Grid.Column="2" Style="{StaticResource ContentCardStyle}">
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="*"/>
                                                        </Grid.RowDefinitions>

                                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                                            <materialDesign:PackIcon Kind="PackageVariant" VerticalAlignment="Center" Margin="0,0,10,0" Opacity="0.5"/>
                                                            <TextBlock Text="{Binding TituloBarrasProductos}" FontWeight="Bold" FontSize="16"/>
                                                        </StackPanel>

                                                        <lvc:CartesianChart Grid.Row="1" 
                                                            Series="{Binding SeriesBarrasProductos}" 
                                                            XAxes="{Binding EjeXBarrasProductos}" 
                                                            YAxes="{Binding EjeYBarrasProductos}"
                                                            ZoomMode="X" AnimationsSpeed="0:0:0.5"/>
                                                    </Grid>
                                                </materialDesign:Card>
                                            </Grid>
                                        </Grid>
                                    </materialDesign:Card>
                                </Grid>
                            </ScrollViewer>
                        </TabItem>

                        <TabItem>
                            <TabItem.Header>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="TableLarge" Margin="0,0,8,0"/>
                                    <TextBlock Text="TABLA DETALLADA"/>
                                </StackPanel>
                            </TabItem.Header>

                            <Grid Margin="15">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Margin="0,0,0,15">
                                    <TextBox Text="{Binding TextoBusqueda, UpdateSourceTrigger=PropertyChanged}"
                                             materialDesign:HintAssist.Hint="Buscar cliente, producto..."
                                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                             Width="400" HorizontalAlignment="Left"
                                             materialDesign:TextFieldAssist.HasClearButton="True"
                                             materialDesign:TextFieldAssist.LeadingIcon="Magnify"/>
                                </StackPanel>

                                <DataGrid Grid.Row="1" ItemsSource="{Binding DetalleVentas}" 
                                          AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" 
                                          HeadersVisibility="Column" materialDesign:DataGridAssist.CellPadding="13 8 8 8" ColumnWidth="*"
                                          EnableRowVirtualization="True" VirtualizingPanel.VirtualizationMode="Recycling">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="CLIENTE" Binding="{Binding Cliente}" Width="2*" FontWeight="SemiBold"/>
                                        <DataGridTextColumn Header="PRODUCTO" Binding="{Binding Descripcion}" Width="2*"/>
                                        <DataGridTextColumn Header="CANTIDAD" Binding="{Binding Cantidad, StringFormat=N0}" Width="Auto">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>
                                        <DataGridTextColumn Header="LITROS" Binding="{Binding LitrosTotales, StringFormat=N0}" Width="Auto">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>
                                        <DataGridTextColumn Header="TOTAL ($)" Binding="{Binding TotalVenta, StringFormat=C2}" FontWeight="Bold" Width="Auto">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                                    <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}"/>
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </TabItem>

                    </TabControl>
                </materialDesign:Card>
            </Grid>
        </materialDesign:TransitioningContent>

        <Grid Background="#80FFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" Panel.ZIndex="100">
            <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" IsIndeterminate="True" Width="60" Height="60"/>
        </Grid>
    </Grid>
</UserControl>