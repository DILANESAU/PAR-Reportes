<UserControl x:Class="WPF_PAR.MVVM.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignPaper}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="InteractiveCard" TargetType="materialDesign:Card">
            <Setter Property="Padding" Value="15"/>
            <Setter Property="UniformCornerRadius" Value="12"/>
            <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp2"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp8"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <ScaleTransform ScaleX="1.02" ScaleY="1.02"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="131*"/>
                        <ColumnDefinition Width="812*"/>
                    </Grid.ColumnDefinitions>
                    <materialDesign:Card Grid.Row="0" Margin="0,0,0,10" Padding="10" UniformCornerRadius="8" Grid.ColumnSpan="2">
                        
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <materialDesign:PackIcon Kind="StoreMarker" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock Text="Dashboard" VerticalAlignment="Center" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,20,0"/>

                            <ComboBox Width="200" Margin="0,0,20,8"
                                materialDesign:HintAssist.Hint="Sucursal"
                                Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                                ItemsSource="{Binding DataContext.ListaSucursales, RelativeSource={RelativeSource AncestorType=Window}}"
                                SelectedValue="{Binding Filters.SucursalId}" 
                                DisplayMemberPath="Value" 
                                SelectedValuePath="Key"/>
                            <DatePicker Width="120" Margin="0,0,10,8" 
                                materialDesign:HintAssist.Hint="Inicio"
                                Style="{StaticResource MaterialDesignFloatingHintDatePicker}"
                                SelectedDate="{Binding Filters.FechaInicio}"/>

                            <DatePicker Width="120" Margin="0,0,20,8" 
                                materialDesign:HintAssist.Hint="Fin"
                                Style="{StaticResource MaterialDesignFloatingHintDatePicker}"
                                SelectedDate="{Binding Filters.FechaFin}"/>

                            <Button Command="{Binding ActualizarCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Content="Actualizar"/>
                        </StackPanel>
                    </materialDesign:Card>
                </Grid>
                <Grid Grid.Row="1" Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <materialDesign:Card Grid.Column="0" Margin="0,0,15,0" Style="{StaticResource InteractiveCard}" Foreground="White">
                        <materialDesign:Card.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#448AFF" Offset="0"/>
                                <GradientStop Color="#1565C0" Offset="1"/>
                            </LinearGradientBrush>
                        </materialDesign:Card.Background>

                        <Grid>
                            <Ellipse Width="100" Height="100" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-20,-20,0" Fill="White" Opacity="0.1"/>

                            <StackPanel>
                                <TextBlock Text="VENTAS TOTALES" Opacity="0.9" FontSize="13" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding TotalVentas, StringFormat=C2}" FontSize="28" FontWeight="Bold" Margin="0,10,0,0">
                                    <TextBlock.Effect>
                                        <DropShadowEffect ShadowDepth="2" BlurRadius="2" Opacity="0.3"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                                <TextBlock Text="Ingresos brutos del periodo" Opacity="0.8" FontSize="11" Margin="0,5,0,0"/>
                            </StackPanel>
                            <materialDesign:PackIcon Kind="CurrencyUsd" HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="40" Height="40" Opacity="0.4" Margin="0,0,10,34" RenderTransformOrigin="0.429,-0.417"/>
                        </Grid>
                    </materialDesign:Card>

                    <materialDesign:Card Grid.Column="1" Margin="0,0,15,0" Style="{StaticResource InteractiveCard}" Foreground="White">
                        <materialDesign:Card.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#FF9800" Offset="0"/>
                                <GradientStop Color="#E38800" Offset="1"/>
                            </LinearGradientBrush>
                        </materialDesign:Card.Background>

                        <Grid>
                            <Ellipse Width="100" Height="100" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-20,-20,0" Fill="White" Opacity="0.1"/>
                            <StackPanel>
                                <TextBlock Text="TRANSACCIONES" Opacity="0.9" FontSize="13" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding CantidadTransacciones}" FontSize="28" FontWeight="Bold" Margin="0,5,0,0">
                                    <TextBlock.Effect>
                                        <DropShadowEffect ShadowDepth="2" BlurRadius="2" Opacity="0.3"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                                <TextBlock Text="Tickets emitidos" Opacity="0.7" FontSize="11" Margin="0,5,0,0"/>
                            </StackPanel>
                            <materialDesign:PackIcon Kind="ReceiptText" HorizontalAlignment="Right" Width="50" Height="50" Opacity="0.2" Margin="0,5,5,0" RenderTransformOrigin="0.68,0.7"/>
                        </Grid>
                    </materialDesign:Card>

                    <materialDesign:Card Grid.Column="2" Style="{StaticResource InteractiveCard}" Foreground="White">
                        <materialDesign:Card.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#4CAF50" Offset="0"/>
                                <GradientStop Color="#59CE5D" Offset="1"/>
                            </LinearGradientBrush>
                        </materialDesign:Card.Background>
                        <Grid>
                            <Ellipse Width="100" Height="100" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-20,-20,0" Fill="White" Opacity="0.1"/>
                            <StackPanel>
                                <TextBlock Text="MEJOR CLIENTE" Opacity="0.9" FontSize="13" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding TopCliente}" FontSize="20" FontWeight="Bold" TextTrimming="CharacterEllipsis" Margin="0,5,0,0" ToolTip="{Binding TopCliente}">
                                    <TextBlock.Effect>
                                        <DropShadowEffect ShadowDepth="2" BlurRadius="2" Opacity="0.3"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                                <TextBlock Text="Mayor volumen de compra" Opacity="0.7" FontSize="11" Margin="0,5,0,0"/>
                            </StackPanel>
                            <materialDesign:PackIcon Kind="Crown" HorizontalAlignment="Right" Width="50" Height="50" Opacity="0.2" Margin="0,5,5,0"/>
                        </Grid>
                    </materialDesign:Card>
                </Grid>

                <materialDesign:Card Grid.Row="2" Margin="0,0,0,20" UniformCornerRadius="8" Padding="15">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="200"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Comportamiento Anual de Ventas" FontWeight="Bold" FontSize="16"/>

                        <lvc:CartesianChart Grid.Row="1"
                                            Series="{Binding SeriesHistorico}"
                                            XAxes="{Binding EjeXHistorico}"
                                            Margin="0,10,0,0"
                                            TooltipTextSize="16"/>
                    </Grid>
                </materialDesign:Card>

                <Grid Grid.Row="3" MinHeight="350">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <materialDesign:Card Grid.Column="0" Margin="0,0,20,0" UniformCornerRadius="8" Padding="0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Border Background="{DynamicResource PrimaryHueMidBrush}" CornerRadius="8 8 0 0" Padding="15">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="TableLarge" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <TextBlock Text="Últimas Ventas Registradas" Foreground="White" FontWeight="Bold" FontSize="14"/>
                                </StackPanel>
                            </Border>

                            <DataGrid Grid.Row="1" 
                                      ItemsSource="{Binding ListaVentas}" 
                                      AutoGenerateColumns="False" 
                                      IsReadOnly="True"
                                      HeadersVisibility="Column"
                                      GridLinesVisibility="Horizontal"
                                      CanUserAddRows="False"
                                      SelectionMode="Single"
                                      VerticalScrollBarVisibility="Auto"
                                      materialDesign:DataGridAssist.CellPadding="10 8"
                                      BorderThickness="0">

                                <DataGrid.Resources>
                                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}"/>
                                    </Style>
                                </DataGrid.Resources>

                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="FECHA" Binding="{Binding Fecha, StringFormat=dd/MMM}" Width="80"/>
                                    <DataGridTextColumn Header="MOVIMIENTO" Binding="{Binding Mov}" Width="140"/>
                                    <DataGridTextColumn Header="CLIENTE" Binding="{Binding Cliente}" Width="*"/>
                                    <DataGridTextColumn Header="TOTAL" Binding="{Binding PrecioTotal, StringFormat=C2}" Width="100" FontWeight="Bold">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                                <Setter Property="Padding" Value="0,0,15,0"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </materialDesign:Card>

                    <materialDesign:Card Grid.Column="1" UniformCornerRadius="8" Padding="15">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Tendencia Diaria (Mes Actual)" FontWeight="Bold" FontSize="16" Foreground="{DynamicResource MaterialDesignBody}"/>

                            <lvc:CartesianChart Grid.Row="1"
                                                Series="{Binding SeriesGrafico}"
                                                XAxes="{Binding EjeX}"
                                                YAxes="{Binding EjeY}"
                                                Margin="0,15,0,0"
                                                TooltipTextSize="16"
                                                TooltipTextPaint="{Binding TooltipTextPaint}"/>
                        </Grid>
                    </materialDesign:Card>
                </Grid>
            </Grid>
        </ScrollViewer>

        <Grid Background="#99FFFFFF" 
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
              d:Visibility="Collapsed">
            <materialDesign:Card UniformCornerRadius="10" Padding="30" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                                 Value="0"
                                 IsIndeterminate="True"
                                 Width="50" Height="50"
                                 Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                    <TextBlock Text="Cargando información..." 
                               HorizontalAlignment="Center" 
                               Margin="0,20,0,0" 
                               FontWeight="Bold"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>