<UserControl x:Class="WPF_PAR.MVVM.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignPaper}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <materialDesign:Card Grid.Row="0" Margin="0,0,0,20" Padding="15" UniformCornerRadius="8">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="ViewDashboard" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="Filtros:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,20,0"/>

                    <ComboBox materialDesign:HintAssist.Hint="Sucursal" Width="200" Margin="0,0,15,0"
                              Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                              ItemsSource="{Binding ListaSucursales}"
                              DisplayMemberPath="Value"
                              SelectedValuePath="Key"
                              SelectedValue="{Binding SucursalSeleccionadaId}"/>

                    <ComboBox materialDesign:HintAssist.Hint="Año" Width="80" Margin="0,0,15,0"
                              Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                              ItemsSource="{Binding ListaAnios}"
                              SelectedItem="{Binding AnioSeleccionado}"/>

                    <ComboBox materialDesign:HintAssist.Hint="Mes" Width="120" Margin="0,0,20,0"
                              Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                              ItemsSource="{Binding ListaMeses}"
                              DisplayMemberPath="Value"
                              SelectedValuePath="Key"
                              SelectedValue="{Binding MesSeleccionado}"/>

                    <Button Content="Actualizar" 
                            Command="{Binding ActualizarCommand}"
                            Height="30" Padding="15,0" Margin="10,0,0,0"/>
                </StackPanel>
            </materialDesign:Card>

            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <materialDesign:Card Grid.Column="0" Margin="0,0,10,0" Background="#2196F3" Foreground="White" Padding="15" UniformCornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="TOTAL VENTAS" Opacity="0.8" FontSize="12" Foreground="White"/>
                        <TextBlock Text="{Binding TotalVentas, StringFormat=C2}" FontSize="24" FontWeight="Bold"/>
                        <materialDesign:PackIcon Kind="CurrencyUsd" HorizontalAlignment="Right" Width="40" Height="40" Margin="0,-40,0,0" Opacity="0.4"/>
                    </StackPanel>
                </materialDesign:Card>

                <materialDesign:Card Grid.Column="1" Margin="5,0,5,0" Background="#FF9800" Foreground="White" Padding="15" UniformCornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="TRANSACCIONES" Opacity="0.8" FontSize="12" Foreground="White"/>
                        <TextBlock Text="{Binding CantidadTransacciones}" FontSize="24" FontWeight="Bold"/>
                        <materialDesign:PackIcon Kind="Receipt" HorizontalAlignment="Right" Width="40" Height="40" Margin="0,-40,0,0" Opacity="0.4"/>
                    </StackPanel>
                </materialDesign:Card>

                <materialDesign:Card Grid.Column="2" Margin="10,0,0,0" Background="#4CAF50" Foreground="White" Padding="15" UniformCornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="MEJOR CLIENTE" Opacity="0.8" FontSize="12" Foreground="White"/>
                        <TextBlock Text="{Binding TopCliente}" FontSize="18" FontWeight="Bold" TextTrimming="CharacterEllipsis" ToolTip="{Binding TopCliente}"/>
                        <materialDesign:PackIcon Kind="AccountStar" HorizontalAlignment="Right" Width="40" Height="40" Margin="0,-35,0,0" Opacity="0.4"/>
                    </StackPanel>
                </materialDesign:Card>
            </Grid>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.5*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <materialDesign:Card Grid.Column="0" Margin="0,0,10,0" VerticalAlignment="Stretch" UniformCornerRadius="8" Padding="5">
                    <DataGrid ItemsSource="{Binding ListaVentas}" 
                              AutoGenerateColumns="False" 
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal"
                              CanUserAddRows="False"
                              SelectionMode="Single"
                              VerticalScrollBarVisibility="Auto">

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy}" Width="100"/>
                            <DataGridTextColumn Header="Movimiento" Binding="{Binding Mov}" Width="120"/>
                            <DataGridTextColumn Header="Cliente" Binding="{Binding Cliente}" Width="*"/>
                            <DataGridTextColumn Header="Total" Binding="{Binding PrecioTotal, StringFormat=C2}" Width="100" FontWeight="Bold"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </materialDesign:Card>

                <materialDesign:Card Grid.Column="1" Margin="10,0,0,0" Padding="15" UniformCornerRadius="8">
                    <DockPanel>
                        <TextBlock Text="Tendencia Diaria" DockPanel.Dock="Top" FontWeight="Bold" Margin="0,0,0,10" FontSize="16"/>
                    </DockPanel>
                </materialDesign:Card>
            </Grid>
        </Grid>

        <Grid Background="#80FFFFFF" 
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
              d:Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                             Value="0"
                             IsIndeterminate="True"
                             Width="60" Height="60"/>

                <TextBlock Text="Consultando datos..." 
                           HorizontalAlignment="Center" 
                           Margin="0,15,0,0" 
                           FontWeight="Bold" 
                           Foreground="{DynamicResource PrimaryHueMidBrush}"/>
            </StackPanel>
        </Grid>

    </Grid>
</UserControl>