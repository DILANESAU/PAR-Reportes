<UserControl x:Class="WPF_PAR.MVVM.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:converters="clr-namespace:WPF_PAR.Core.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1280"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="#F0F2F5" 
             FontFamily="{DynamicResource MaterialDesignFont}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis"/>
        <converters:BoolToArrowIconConverter x:Key="BoolToArrowIconConverter"/>

        <Style x:Key="GrowthTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#D32F2F"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding EsCrecimientoPositivo}" Value="True">
                    <Setter Property="Foreground" Value="#2E7D32"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="GrowthIconStyle" TargetType="materialDesign:PackIcon">
            <Setter Property="Foreground" Value="#D32F2F"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding EsCrecimientoPositivo}" Value="True">
                    <Setter Property="Foreground" Value="#2E7D32"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <DropShadowEffect x:Key="SombraSuave" BlurRadius="15" ShadowDepth="2" Direction="270" Opacity="0.1" Color="Black"/>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
            <StackPanel MaxWidth="1600" HorizontalAlignment="Center">

                <Grid Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="Tablero Principal" Style="{StaticResource MaterialDesignHeadline4TextBlock}" FontWeight="Bold"/>
                        <TextBlock Text="Resumen de rendimiento y operaciones" Opacity="0.5" FontSize="14" Margin="0,5,0,0"/>
                    </StackPanel>

                    <materialDesign:Card Grid.Column="1" UniformCornerRadius="8" Padding="5" VerticalAlignment="Center" Effect="{StaticResource SombraSuave}">
                        <StackPanel Orientation="Horizontal">
                            <ComboBox ItemsSource="{Binding Filters.ListaSucursales}" 
                                      SelectedValue="{Binding Filters.SucursalId}"
                                      DisplayMemberPath="Value" SelectedValuePath="Key"
                                      Width="180" Margin="10,0" 
                                      materialDesign:HintAssist.Hint="Sucursal"
                                      materialDesign:TextFieldAssist.UnderlineBrush="{DynamicResource PrimaryHueMidBrush}"
                                      Style="{StaticResource MaterialDesignFilledComboBox}"/>

                            <ComboBox ItemsSource="{Binding ListaPeriodos}"
                                      SelectedItem="{Binding PeriodoSeleccionado}"
                                      Width="140" Margin="0,0,10,0"
                                      materialDesign:HintAssist.Hint="Periodo"
                                      Style="{StaticResource MaterialDesignFilledComboBox}"/>

                            <Button Command="{Binding ActualizarCommand}"
                                    Style="{StaticResource MaterialDesignIconButton}" ToolTip="Actualizar Datos" Width="40" Height="40">
                                <materialDesign:PackIcon Kind="Refresh" Width="20" Height="20"/>
                            </Button>
                        </StackPanel>
                    </materialDesign:Card>
                </Grid>

                <Grid Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <materialDesign:Card Grid.Column="0" UniformCornerRadius="12" Padding="25" Effect="{StaticResource SombraSuave}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border Background="#E8F5E9" CornerRadius="12" Width="60" Height="60">
                                <materialDesign:PackIcon Kind="CurrencyUsd" Width="30" Height="30" Foreground="#2E7D32" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>

                            <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="INGRESOS TOTALES" FontSize="13" Opacity="0.5" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding GranTotalVenta, StringFormat=C0}" FontSize="32" FontWeight="Bold" Foreground="#1A1A1A"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <materialDesign:PackIcon Kind="{Binding EsCrecimientoPositivo, Converter={StaticResource BoolToArrowIconConverter}}" 
                                                             Style="{StaticResource GrowthIconStyle}"
                                                             Width="14" Height="14" VerticalAlignment="Center"/>

                                    <TextBlock Text="{Binding VariacionPorcentaje, StringFormat=P0}" 
                                               Style="{StaticResource GrowthTextStyle}"
                                               FontWeight="Bold" FontSize="12" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="vs periodo ant." FontSize="10" Opacity="0.4" HorizontalAlignment="Right" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Grid>
                    </materialDesign:Card>

                    <materialDesign:Card Grid.Column="2" UniformCornerRadius="12" Padding="25" Effect="{StaticResource SombraSuave}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Background="#E3F2FD" CornerRadius="12" Width="60" Height="60">
                                <materialDesign:PackIcon Kind="WaterOutline" Width="30" Height="30" Foreground="#1565C0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>

                            <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="VOLUMEN VENDIDO" FontSize="13" Opacity="0.5" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding GranTotalLitros, StringFormat={}{0:N0} Litros}" FontSize="32" FontWeight="Bold" Foreground="#1A1A1A"/>
                            </StackPanel>
                        </Grid>
                    </materialDesign:Card>
                </Grid>

                <Grid Height="500">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.2*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <materialDesign:Card Grid.Column="0" UniformCornerRadius="12" Padding="0" Effect="{StaticResource SombraSuave}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Border BorderBrush="#EEEEEE" BorderThickness="0,0,0,1" Padding="20">
                                <Grid>
                                    <StackPanel>
                                        <TextBlock Text="Distribución por Familia" FontWeight="Bold" FontSize="16"/>
                                        <TextBlock Text="Participación de venta por categoría" FontSize="12" Opacity="0.5"/>
                                    </StackPanel>
                                    <materialDesign:PackIcon Kind="ChartPie" HorizontalAlignment="Right" Opacity="0.1" Width="40" Height="40"/>
                                </Grid>
                            </Border>

                            <Grid Grid.Row="1" Margin="20">
                                <lvc:PieChart Series="{Binding SeriesFamilias}" 
                                              LegendPosition="Right"
                                              InitialRotation="-90"
                                              TooltipTextSize="14"
                                              Visibility="{Binding TieneDatos, Converter={StaticResource BoolToVis}}"/>

                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                                            Visibility="{Binding TieneDatos, Converter={StaticResource InverseBoolToVis}}">
                                    <materialDesign:PackIcon Kind="ChartDonut" Width="60" Height="60" Foreground="#DDDDDD"/>
                                    <TextBlock Text="Sin datos para mostrar" Foreground="#AAAAAA" Margin="0,10,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </materialDesign:Card>

                    <materialDesign:Card Grid.Column="2" UniformCornerRadius="12" Padding="0" Effect="{StaticResource SombraSuave}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Border BorderBrush="#EEEEEE" BorderThickness="0,0,0,1" Padding="20">
                                <Grid>
                                    <StackPanel>
                                        <TextBlock Text="Top Clientes" FontWeight="Bold" FontSize="16"/>
                                        <TextBlock Text="Los 10 mejores clientes del periodo" FontSize="12" Opacity="0.5"/>
                                    </StackPanel>
                                    <materialDesign:PackIcon Kind="TrophyOutline" HorizontalAlignment="Right" Opacity="0.1" Width="40" Height="40"/>
                                </Grid>
                            </Border>

                            <DataGrid Grid.Row="1" ItemsSource="{Binding TopClientesList}" 
                                      AutoGenerateColumns="False" 
                                      CanUserAddRows="False" 
                                      IsReadOnly="True"
                                      HeadersVisibility="Column"
                                      GridLinesVisibility="Horizontal"
                                      HorizontalGridLinesBrush="#F0F0F0"
                                      BorderThickness="0"
                                      Background="White"
                                      materialDesign:DataGridAssist.CellPadding="12"
                                      ScrollViewer.VerticalScrollBarVisibility="Auto">

                                <DataGrid.Resources>
                                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Opacity" Value="0.6"/>
                                        <Setter Property="FontSize" Value="11"/>
                                    </Style>
                                </DataGrid.Resources>

                                <DataGrid.Columns>
                                    <DataGridTemplateColumn Header="#" Width="Auto">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border Background="#F5F5F5" CornerRadius="4" Width="24" Height="24">
                                                    <TextBlock Text="{Binding Ranking}" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" FontSize="11" Opacity="0.6"/>
                                                </Border>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTextColumn Header="CLIENTE" Binding="{Binding Nombre}" Width="*" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}">
                                        <DataGridTextColumn.CellStyle>
                                            <Style TargetType="DataGridCell" BasedOn="{StaticResource MaterialDesignDataGridCell}">
                                                <Setter Property="ToolTip" Value="{Binding Nombre}"/>
                                            </Style>
                                        </DataGridTextColumn.CellStyle>
                                    </DataGridTextColumn>

                                    <DataGridTextColumn Header="TICKETS" Binding="{Binding Transacciones}" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>

                                    <DataGridTextColumn Header="VENTA" Binding="{Binding Monto, StringFormat=C0}" FontWeight="Bold" Foreground="#2E7D32" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </materialDesign:Card>
                </Grid>

            </StackPanel>
        </ScrollViewer>

        <Grid Background="#90FFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" 
                             IsIndeterminate="True" 
                             Width="50" Height="50" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                <TextBlock Text="Procesando Datos..." Margin="0,15,0,0" FontWeight="SemiBold" Opacity="0.6" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>