<UserControl x:Class="WPF_PAR.MVVM.Views.ClientesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="Transparent">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="Cartera de Clientes" Style="{StaticResource PageTitleStyle}"/>
            <TextBlock Text="Análisis detallado de comportamiento y tendencias anuales" Style="{StaticResource PageSubtitleStyle}"/>
        </StackPanel>

        <materialDesign:Card Grid.Row="1" Style="{StaticResource FilterBarStyle}">
            <StackPanel Orientation="Horizontal">
                <ComboBox ItemsSource="{Binding Filters.ListaSucursales}" 
                          SelectedValue="{Binding Filters.SucursalId}"
                          DisplayMemberPath="Value" SelectedValuePath="Key"
                          materialDesign:HintAssist.Hint="Sucursal" Width="220" Margin="0,0,20,0"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"/>

                <ComboBox ItemsSource="{Binding AñosDisponibles}"
                          SelectedItem="{Binding AnioSeleccionado}"
                          materialDesign:HintAssist.Hint="Año Fiscal" Width="120" Margin="0,0,20,0"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"/>

                <TextBox Text="{Binding TextoBusqueda, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="Buscar cliente..."
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Width="300" Margin="20,0,0,0" VerticalAlignment="Top"/>

                <Button Command="{Binding ActualizarCommand}" Content="CONSULTAR" 
                        Style="{StaticResource MaterialDesignRaisedButton}" Margin="20,0,0,0"/>
            </StackPanel>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="2" Style="{StaticResource ContentCardStyle}" Padding="0">
            <DataGrid ItemsSource="{Binding ListaClientes}" 
                      Style="{StaticResource ModernDataGrid}"
                      FrozenColumnCount="1">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="CLIENTE" Binding="{Binding Nombre}" Width="250" FontWeight="SemiBold"/>
                    <DataGridTextColumn Header="ENE" Binding="{Binding Enero, StringFormat=N0}" ElementStyle="{StaticResource CellMoney}"/>
                    <DataGridTemplateColumn Header="TOTAL" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TotalAnual, StringFormat=C0}" FontWeight="Bold" HorizontalAlignment="Right"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>

        <Grid Grid.Row="2" Background="#90FFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
            <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" IsIndeterminate="True" Width="50" Height="50"/>
        </Grid>
    </Grid>
</UserControl>