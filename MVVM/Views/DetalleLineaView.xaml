<UserControl x:Class="WPF_PAR.MVVM.Views.DetalleLineaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Height="600" Width="900">
    <materialDesign:Card Padding="20" UniformCornerRadius="8" Background="{DynamicResource MaterialDesignPaper}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="ChartBox" Width="30" Height="30" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <StackPanel>
                        <TextBlock Text="{Binding TituloDetalle}" Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
                        <TextBlock Text="Desglose completo de movimientos" Opacity="0.6"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">

                    <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                            Command="{Binding ExportarExcelCommand}" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FileExcel" Margin="0,0,5,0"/>
                            <TextBlock Text="Excel"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource MaterialDesignIconForegroundButton}"
                            Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                            ToolTip="Cerrar">
                        <materialDesign:PackIcon Kind="Close"/>
                    </Button>
                </StackPanel>
            </Grid>

            <DataGrid Grid.Row="1" ItemsSource="{Binding DetalleVentas}"
                      AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      SelectionMode="Single" VerticalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Fecha" Binding="{Binding FechaEmision, StringFormat=dd/MM/yyyy}" Width="100"/>
                    <DataGridTextColumn Header="Sucursal" Binding="{Binding Sucursal}" Width="80"/>
                    <DataGridTextColumn Header="Folio" Binding="{Binding MovID}" Width="100"/>
                    <DataGridTextColumn Header="Clave" Binding="{Binding Articulo}" Width="100" FontWeight="SemiBold"/>
                    <DataGridTextColumn Header="Producto" Binding="{Binding Descripcion}" Width="200"/>
                    <DataGridTextColumn Header="Cliente" Binding="{Binding Cliente}" Width="180"/>
                    <DataGridTextColumn Header="Cant." Binding="{Binding Cantidad, StringFormat=N0}" Width="60"/>
                    <DataGridTextColumn Header="Lts" Binding="{Binding LitrosTotales, StringFormat=N1}" Width="70"/>
                    <DataGridTextColumn Header="Precio" Binding="{Binding PrecioNeto, StringFormat=C2}" Width="90"/>
                    <DataGridTextColumn Header="Total" Binding="{Binding TotalVenta, StringFormat=C2}" Width="110" FontWeight="Bold"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </materialDesign:Card>
</UserControl>